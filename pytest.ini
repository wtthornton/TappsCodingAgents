[pytest]
# Pytest configuration for TappsCodingAgents

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Add current directory to Python path
pythonpath = .

# Output options
# Performance optimizations:
# - Coverage disabled by default (use --cov to enable)
# - Only unit tests run by default (use -m "" to run all tests)
# - Use -n auto for parallel execution (requires pytest-xdist: pip install pytest-xdist)
#
# Fast run (default): pytest tests/
# All tests: pytest tests/ -m ""
# With coverage: pytest tests/ --cov=tapps_agents --cov-report=term
# Parallel: pytest tests/ -n auto
addopts =
    -v
    --strict-markers
    --strict-config
    --tb=short
    --asyncio-mode=auto
    -m unit

# Markers
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (slower, with dependencies)
    e2e: End-to-end tests (slowest, full system)
    slow: Tests that take longer than 5 seconds
    requires_ollama: Tests that require Ollama to be running
    requires_llm: Tests that require any LLM service (Ollama, Anthropic, or OpenAI)
    requires_context7: Tests that require Context7 API key and make real API calls

# Async configuration
asyncio_mode = auto

# Coverage options
[coverage:run]
source = tapps_agents
omit =
    */tests/*
    */__pycache__/*
    */site-packages/*
    */cli.py  # CLI tests will be added in later phase

[coverage:report]
precision = 2
show_missing = True
skip_covered = False

[coverage:html]
directory = htmlcov

