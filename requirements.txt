# Core Framework
pydantic>=2.10.0
httpx>=0.28.0
pyyaml>=6.0.2

# LLM Integration (Ollama)
aiohttp>=3.9.0

# Code Analysis (for scoring)
radon>=6.0.1          # Complexity analysis
bandit>=1.7.5         # Security analysis
pylint>=3.0.0         # Code quality (optional, for maintainability)
coverage>=7.0.0       # Test coverage analysis

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-timeout>=2.2.0

# Development
black>=24.0.0
# Code Quality Tools (2025 Standards - Phase 6)
ruff>=0.8.0,<1.0           # Fast Python linter (10-100x faster than pylint)
mypy>=1.13.0,<2.0          # Static type checking
pip-audit>=2.6.0           # Dependency security auditing
pipdeptree>=2.5.0          # Dependency tree visualization

# Reporting (Phase 6.3)
jinja2>=3.1.0              # HTML template rendering for quality reports
plotly>=5.18.0             # Optional: Trend visualization (for future enhancements)
