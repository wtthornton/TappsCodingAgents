# Requirements file (convenience artifact)
# NOTE: This file is non-authoritative. pyproject.toml is the single source of truth.
# This file matches: dependencies + dev extras from pyproject.toml
# See docs/DEPENDENCY_POLICY.md for the dependency management policy.

# Core Framework
pydantic>=2.12.0
httpx>=0.28.0
pyyaml>=6.0.3
aiohttp>=3.13.2
psutil>=7.1.0

# Code Analysis (for scoring)
radon>=6.0.1
bandit>=1.9.2
coverage>=7.13.0

# Reporting
jinja2>=3.1.6
plotly>=6.5.0

# CLI UX (progress spinners/bars, live updates)
rich>=13.9.4

# Dependency conflict resolution
# Constrain packaging to avoid conflicts with transitive dependencies (e.g., langchain-core)
# langchain-core 0.2.43 requires packaging<25,>=23.2
packaging>=23.2,<25

# Development Tools (from dev extra)
black>=25.12.0
ruff>=0.14.10,<1.0
mypy>=1.19.1,<2.0
types-PyYAML>=6.0.12

# Testing
pytest>=9.1.0
pytest-asyncio>=1.3.0
pytest-cov>=7.0.0
pytest-mock>=3.15.1
pytest-timeout>=2.4.0
pytest-xdist>=3.8.0
pytest-sugar>=1.1.1
pytest-html>=4.1.1
pytest-rich>=0.2.0

# Code Quality (optional, for maintainability)
pylint>=4.0.4

# Security & Dependency Management
pip-audit>=2.10.0
# Note: pipdeptree moved to [dependency-analysis] extra due to packaging>=25 requirement
# which conflicts with our packaging<25 constraint (needed for langchain-core compatibility)
# To install: pip install -e ".[dependency-analysis]"
# Lock File Generation
pip-tools>=7.5.2
