{
  "library": "analytics_dashboard",
  "topic": "best-practices",
  "documentation": {
    "content": "## CSS Modules for Component Styling\n\n```css\n/* StatsCard.module.css */\n.title {\n  font-weight: 700;\n  text-transform: uppercase;\n  color: var(--mantine-color-dimmed);\n}\n\n.value {\n  font-size: 2rem;\n  line-height: 1;\n}\n\n.diff {\n  display: flex;\n  align-items: center;\n  gap: 0.25rem;\n}\n```\n\n## CSS Modules for Component Styling\n\n```typescript\n// StatsCard.tsx\nimport classes from './StatsCard.module.css';\n\nconst StatsCard = ({ data }: Props) => (\n  <div>\n    <Text className={classes.title}>{data.title}</Text>\n    <Text className={classes.value}>{data.value}</Text>\n  </div>\n);\n```\n\n## Build Project for Production\n\n```bash\nnpm run build\n```\n\n## Default vs Named Exports Strategy\n\n```typescript\n// Component file\nconst StatsCard = ({ data }: StatsCardProps) => {\n  // ...\n};\n\nexport default StatsCard;\nexport type { StatsCardProps };\nexport { STATS_CARD_VARIANTS };\n```\n\n## Essential Development Commands (Bash)\n\n```bash\nnpm run dev\nnpm run build\nnpm start\nnpm run lint\nnpm run prettier\nnpm run storybook\nnpm run build-storybook\nnpm run changeset:add\nnpm run changeset:release\nnpm run commitlint\n```\n\n## API Integration and RBAC Usage\n\n```typescript\nimport { useInvoicesWithMutations, PermissionGate } from '@/lib/api';\n\nfunction MyComponent() {\n  const { data, mutations } = useInvoicesWithMutations();\n\n  return (\n    <PermissionGate permission=\"Permissions.Personal.Invoices\">\n      <InvoiceManager data={data} onCreate={mutations.create} />\n    </PermissionGate>\n  );\n}\n```\n\n## Combine Permission Hooks with API Hooks (React)\n\n```typescript\n// \u2705 Good - API hook handles permissions automatically\nconst { data, mutations } = useInvoicesWithMutations();\n\n// \u274c Bad - manual permission checking\nconst canAccess = useHasPermission('Permissions.Personal.Invoices');\nconst data = canAccess ? useFetch('/api/invoices') : null;\n```\n\n## TypeScript: Defining Props with Generics and Mantine Integration\n\n```typescript\nimport { PaperProps } from '@mantine/core';\nimport { components } from '@/lib/api';\n\ntype Invoice = components['schemas']['Invoice'];\n\ntype InvoiceCardProps = {\n  invoice: Invoice;\n  onEdit?: (id: string) => void;\n  loading?: boolean;\n} & PaperProps;\n```\n\n## RBAC Component Examples\n\n```typescript\n// Permission-based UI rendering\n<PermissionGate permission=\"Permissions.Team.Projects\">\n  <ProjectManager />\n</PermissionGate>\n\n// Multiple permission checks\n<MultiPermissionGate\n  permissions={['Permissions.Team.Projects', 'Permissions.Team.Orders']}\n  mode=\"any\"\n>\n  <TeamDashboard />\n</MultiPermissionGate>\n\n// Admin-only content\n<AdminOnly>\n  <UserManagementPanel />\n</AdminOnly>\n\n// Permission hooks\nconst canCreateInvoices = useHasPermission('Permissions.Personal.Invoices');\nconst isAdmin = useIsAdmin();\n```\n\n## Projects API Hooks for Team Collaboration\n\n```typescript\nimport { useProjectsWithMutations } from '@/lib/api/hooks/projects';\n\nconst { data, loading, error, mutations } = useProjectsWithMutations();\n\n// Create project\nawait mutations.create({ name, description });\n\n// Update project\nawait mutations.update(id, { name: \"Updated\" });\n\n// Delete project\nawait mutations.delete(id);\n```\n\n## Use Type-Safe API Hooks with TypeScript\n\n```typescript\nimport { useInvoicesWithMutations } from '@/lib/api/hooks/invoices';\n\nfunction InvoicesPage() {\n  const { data, loading, error, mutations } = useInvoicesWithMutations();\n\n  const handleCreate = async () => {\n    await mutations.create({\n      title: \"New Invoice\",\n      amount: 100\n    }); // \u2705 Fully typed with autocomplete\n  };\n\n  if (loading) return <div>Loading...</div>;\n  if (error) return <div>Error: {error.message}</div>;\n\n  return (\n    <div>\n      {data?.data?.map(invoice => (\n        <div key={invoice.id}>{invoice.title}</div>\n      ))}\n      <button onClick={handleCreate}>Create Invoice</button>\n    </div>\n  );\n}\n```\n\nContext7 cache for analytics_dashboard: cached at 2026-01-31T02:29:21.027349+00:00Z; consider refreshing if docs changed.",
    "library": "analytics_dashboard",
    "topic": "best-practices",
    "source": "cache"
  },
  "source": "cache",
  "saved_at": "2026-01-30T18:29:21.101935"
}