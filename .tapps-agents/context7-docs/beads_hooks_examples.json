{
  "library": "beads_hooks",
  "topic": "examples",
  "documentation": {
    "content": "## useDynamicList Basic Usage Example\n\n```javascript\n// Example demonstrating basic list management with useDynamicList\n// This snippet is illustrative and requires a React environment with ahooks imported.\n\n// import { useDynamicList } from 'ahooks';\n\n// const { list, push, remove, getKey } = useDynamicList(['David', 'Jack']);\n\n// // To render:\n// <ul>\n//   {list.map(item => (\n//     <li key={getKey(item)}>\n//       {item}\n//       <button onClick={() => remove(getKey(item))}>Remove</button>\n//     </li>\n//   ))}\n// </ul>\n// <button onClick={() => push('New User')}>Add User</button>\n\n// The provided text shows UI elements like 'Remove odd items' and 'Remove even items',\n// implying more complex list manipulation which would involve custom logic using the hook's methods.\n// The output `[['David','Jack']]` likely represents the state of the list after some operations.\n```\n\n## useCounter Basic Usage Example\n\n```javascript\nimport { useCounter } from 'ahooks';\n\nfunction CounterComponent() {\n  const [current, { inc, dec, set, reset }] = useCounter(0, { min: 0, max: 10 });\n\n  return (\n    <div>\n      <p>Current: {current}</p>\n      <button onClick={() => inc()}>Increment</button>\n      <button onClick={() => dec()}>Decrement</button>\n      <button onClick={() => set(5)}>Set to 5</button>\n      <button onClick={() => reset()}>Reset</button>\n    </div>\n  );\n}\n```\n\n## useNetwork Basic Usage Example\n\n```javascript\nimport { useNetwork } from 'ahooks';\n\nconst NetworkStatus = () => {\n  const networkState = useNetwork();\n\n  return (\n    <div>\n      <p>Network Status: {networkState.online ? 'Online' : 'Offline'}</p>\n      <p>RTT: {networkState.rtt}ms</p>\n      <p>Save Data: {networkState.saveData ? 'Enabled' : 'Disabled'}</p>\n      <p>Downlink: {networkState.downlink} Mbps</p>\n      <p>Effective Type: {networkState.effectiveType}</p>\n    </div>\n  );\n};\n\nexport default NetworkStatus;\n```\n\n## useBoolean Basic Usage Demo\n\n```javascript\nimport { useBoolean } from 'ahooks';\n\nconst MyComponent = () => {\n  const [state, { toggle, setTrue, setFalse }] = useBoolean(false);\n\n  return (\n    <div>\n      <p>Current state: {state ? 'true' : 'false'}</p>\n      <button onClick={toggle}>Toggle</button>\n      <button onClick={setTrue}>Set True</button>\n      <button onClick={setFalse}>Set False</button>\n    </div>\n  );\n};\n\n```\n\n## useInterval Basic Usage Example\n\n```JavaScript\nimport { useInterval } from 'ahooks';\nimport React, { useState } from 'react';\n\nexport default () => {\n  const [count, setCount] = useState(0);\n\n  useInterval(() => {\n    setCount(c => c + 1);\n  }, 1000);\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n    </div>\n  );\n};\n```\n\n## useResetState Basic Usage Example\n\n```javascript\nimport { useResetState } from 'ahooks';\n\nconst MyComponent = () => {\n  const [state, setState, resetState] = useResetState({\n    hello: '',\n    value: 0.47052288677291243,\n  });\n\n  // Example of updating state\n  const updateState = () => {\n    setState({\n      hello: 'world',\n      value: 1.2345,\n    });\n  };\n\n  // Example of resetting state\n  const handleReset = () => {\n    resetState();\n  };\n\n  return (\n    <div>\n      <p>Hello: {state.hello}</p>\n      <p>Value: {state.value}</p>\n      <button onClick={updateState}>Set Hello and Value</button>\n      <button onClick={handleReset}>Reset State</button>\n    </div>\n  );\n};\n\n```\n\n## useTrackedEffect Basic Usage Example\n\n```javascript\nimport { useTrackedEffect } from 'ahooks';\nimport React, { useState } from 'react';\n\nconst Demo = () => {\n  const [count, setCount] = useState(0);\n  const [count2, setCount2] = useState(0);\n\n  useTrackedEffect(\n    (changes, previousDeps, currentDeps) => {\n      console.log('Effect triggered!');\n      console.log('Changes:', changes);\n      console.log('Previous Deps:', previousDeps);\n      console.log('Current Deps:', currentDeps);\n    },\n    [count, count2]\n  );\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <button onClick={() => setCount(count + 1)}>count + 1</button>\n      <p>Count2: {count2}</p>\n      <button onClick={() => setCount2(count2 + 1)}>count2 + 1</button>\n    </div>\n  );\n};\n\nexport default Demo;\n```\n\n## Basic useUrlState Usage\n\n```javascript\nimport { useUrlState } from 'ahooks';\n\nconst MyComponent = () => {\n  const [state, setState] = useUrlState(initialState, options);\n  // ... component logic\n  return (\n    // ... JSX\n  );\n};\n```\n\n## useWhyDidYouUpdate Basic Usage Example\n\n```javascript\nimport React, { useState } from 'react';\nimport { useWhyDidYouUpdate } from 'ahooks';\n\nconst MyComponent = (props) => {\n  const [count, setCount] = useState(0);\n  const [text, setText] = useState('hello');\n\n  // Observe changes in props and state\n  useWhyDidYouUpdate('MyComponent', { props, count, text });\n\n  return (\n    <div>\n      <p>Count: {count}</p>\n      <p>Text: {text}</p>\n      <button onClick={() => setCount(count + 1)}>Increment Count</button>\n      <button onClick={() => setText('world')}>Change Text</button>\n    </div>\n  );\n};\n\nexport default MyComponent;\n```\n\n## useInfiniteScroll Basic Usage\n\n```javascript\nconst { data, loading, loadingMore, loadMore } = useInfiniteScroll(service);\n```",
    "library": "beads_hooks",
    "topic": "usage",
    "source": "fuzzy_match"
  },
  "source": "fuzzy_match",
  "saved_at": "2026-01-29T22:14:18.658970"
}