{
  "library": "hooks",
  "topic": "usage",
  "documentation": {
    "content": "## Track Scroll Direction with useScrollDirection Hook\n\n```tsx\nimport { useScrollDirection } from '@/hookas/use-scroll-direction';\n\nfunction AutoHideNav() {\n  const direction = useScrollDirection();\n\n  return (\n    <nav\n      style={{\n        position: 'fixed',\n        top: 0,\n        transform: direction === 'down' ? 'translateY(-100%)' : 'translateY(0)',\n        transition: 'transform 0.3s'\n      }}\n    >\n      <a href=\"/\">Home</a>\n      <a href=\"/about\">About</a>\n      <a href=\"/contact\">Contact</a>\n    </nav>\n  );\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-scroll-direction.json\n```\n\n## useAsyncEffect: Run Async Operations in Effects with Cleanup (React)\n\n```tsx\nimport { useState } from 'react'\nimport { useAsyncEffect } from '@/hookas/use-async-effect'\n\nfunction UserProfile({ userId }: { userId: string }) {\n  const [user, setUser] = useState(null)\n  const [loading, setLoading] = useState(true)\n\n  useAsyncEffect(async () => {\n    setLoading(true)\n    const response = await fetch(`/api/users/${userId}`)\n    const data = await response.json()\n    setUser(data)\n    setLoading(false)\n\n    // Optional async cleanup\n    return async () => {\n      await fetch(`/api/analytics/exit/${userId}`, { method: 'POST' })\n    }\n  }, [userId])\n\n  if (loading) return <div>Loading...</div>\n  return <div>{user?.name}</div>\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-async-effect.json\n```\n\n## useElementSize Hook for Measuring Element Dimensions (React)\n\n```tsx\nimport { useRef } from 'react'\nimport { useElementSize } from '@/hookas/use-element-size'\n\nfunction ResizableBox() {\n  const ref = useRef(null)\n  const { width, height } = useElementSize(ref)\n\n  return (\n    <div ref={ref}>\n      <p>\n        Width:\n        {width}\n        px\n      </p>\n      <p>\n        Height:\n        {height}\n        px\n      </p>\n    </div>\n  )\n}\n```\n\n## useQuery: Lightweight Data Fetching with Status Tracking (React)\n\n```tsx\nimport { useQuery } from '@/hookas/use-query'\n\nfunction ProductList() {\n  const { data, error, status, refetch } = useQuery(() =>\n    fetch('/api/products').then(r => r.json())\n  )\n\n  if (status === 'loading') return <div>Loading products...</div>\n  if (status === 'error') return <div>Error: {error?.message}</div>\n\n  return (\n    <div>\n      <button onClick={refetch}>Refresh</button>\n      <ul>\n        {data?.map((product: any) => (\n          <li key={product.id}>{product.name} - ${product.price}</li>\n        ))}\n      </ul>\n    </div>\n  )\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-query.json\n```\n\n## Use CSS Media Queries with useMediaQuery Hook\n\n```tsx\nimport { useMediaQuery } from '@/hookas/use-media-query'\n\nfunction ResponsiveComponent() {\n  const isMobile = useMediaQuery('(max-width: 768px)')\n  const isTablet = useMediaQuery('(min-width: 769px) and (max-width: 1024px)')\n  const isDark = useMediaQuery('(prefers-color-scheme: dark)')\n  const isLandscape = useMediaQuery('(orientation: landscape)')\n  const prefersReducedMotion = useMediaQuery('(prefers-reduced-motion: reduce)')\n\n  return (\n    <div className={isDark ? 'dark-theme' : 'light-theme'}>\n      <p>Device: {isMobile ? 'Mobile' : isTablet ? 'Tablet' : 'Desktop'}</p>\n      <p>Orientation: {isLandscape ? 'Landscape' : 'Portrait'}</p>\n      {!prefersReducedMotion && <AnimatedContent />}\n    </div>\n  )\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-media-query.json\n```\n\n## useWindowSize Hook for Retrieving Window Dimensions (React)\n\n```tsx\nimport { useWindowSize } from '@/hookas/use-window-size'\n\nfunction WindowSizeDisplay() {\n  const { width, height } = useWindowSize()\n\n  return (\n    <div>\n      <p>\n        Width:\n        {width}\n        px\n      </p>\n      <p>\n        Height:\n        {height}\n        px\n      </p>\n    </div>\n  )\n}\n```\n\n## useSessionStorage: Persist State in Browser Session Storage (React)\n\n```tsx\nimport { useSessionStorage } from '@/hookas/use-session-storage'\n\nfunction FormDraft() {\n  const [draft, setDraft] = useSessionStorage('form-draft', {\n    title: '',\n    content: '',\n    tags: []\n  })\n\n  return (\n    <form onSubmit={(e) => {\n      e.preventDefault()\n      saveDraft(draft)\n      setDraft({ title: '', content: '', tags: [] })\n    }}>\n      <input\n        value={draft.title}\n        onChange={(e) => setDraft({ ...draft, title: e.target.value })}\n        placeholder=\"Title\"\n      />\n      <textarea\n        value={draft.content}\n        onChange={(e) => setDraft({ ...draft, content: e.target.value })}\n        placeholder=\"Content\"\n      />\n      <button type=\"submit\">Save</button>\n    </form>\n  )\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-session-storage.json\n```\n\n## Control HTML5 Media Elements with useMediaControls Hook\n\n```tsx\nimport { useRef } from 'react'\nimport { useMediaControls } from '@/hookas/use-media-controls'\n\nfunction VideoPlayer({ src }: { src: string }) {\n  const videoRef = useRef<HTMLVideoElement>(null)\n  const {\n    play,\n    pause,\n    toggle,\n    stop,\n    toggleMute,\n    setVolume,\n    setCurrentTime,\n    isPlaying,\n    isMuted,\n    volume,\n    currentTime,\n    duration,\n  } = useMediaControls(videoRef)\n\n  return (\n    <div className=\"video-player\">\n      <video ref={videoRef} src={src} />\n\n      <div className=\"controls\">\n        <button onClick={toggle}>{isPlaying ? 'Pause' : 'Play'}</button>\n        <button onClick={stop}>Stop</button>\n        <button onClick={toggleMute}>{isMuted ? 'Unmute' : 'Mute'}</button>\n\n        <input\n          type=\"range\"\n          min=\"0\"\n          max=\"1\"\n          step=\"0.01\"\n          value={volume}\n          onChange={(e) => setVolume(Number(e.target.value))}\n        />\n\n        <input\n          type=\"range\"\n          min=\"0\"\n          max={duration || 0}\n          value={currentTime}\n          onChange={(e) => setCurrentTime(Number(e.target.value))}\n        />\n\n        <span>{formatTime(currentTime)} / {formatTime(duration)}</span>\n      </div>\n    </div>\n  )\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-media-controls.json\n```\n\n## useLocalStorage: Persist State in Browser Local Storage (React)\n\n```tsx\nimport { useLocalStorage } from '@/hookas/use-local-storage'\n\nfunction UserPreferences() {\n  const [theme, setTheme] = useLocalStorage('theme', 'light')\n  const [fontSize, setFontSize] = useLocalStorage('fontSize', 16)\n  const [notifications, setNotifications] = useLocalStorage('notifications', true)\n\n  return (\n    <div style={{ fontSize: `${fontSize}px` }} className={theme}>\n      <select value={theme} onChange={(e) => setTheme(e.target.value)}>\n        <option value=\"light\">Light</option>\n        <option value=\"dark\">Dark</option>\n      </select>\n\n      <input\n        type=\"range\"\n        min=\"12\"\n        max=\"24\"\n        value={fontSize}\n        onChange={(e) => setFontSize(Number(e.target.value))}\n      />\n\n      <label>\n        <input\n          type=\"checkbox\"\n          checked={notifications}\n          onChange={(e) => setNotifications(e.target.checked)}\n        />\n        Enable notifications\n      </label>\n    </div>\n  )\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-local-storage.json\n```\n\n## useIsomorphicEffect - SSR-Safe useLayoutEffect (React)\n\n```tsx\nimport { useState, useRef } from 'react'\nimport { useIsomorphicEffect } from '@/hookas/use-isomorphic-effect'\n\nfunction MeasuredComponent() {\n  const [height, setHeight] = useState(0)\n  const ref = useRef(null)\n\n  // Runs synchronously on client, safely on server\n  useIsomorphicEffect(() => {\n    if (ref.current) {\n      setHeight(ref.current.offsetHeight)\n    }\n  }, [])\n\n  return (\n    <div ref={ref}>\n      <p>This element is {height}px tall</p>\n    </div>\n  )\n}\n\n// Install: npx shadcn@latest add https://hookas.letstri.dev/r/use-isomorphic-effect.json\n```\n",
    "library": "hooks",
    "topic": "usage",
    "source": "api"
  },
  "source": "api",
  "saved_at": "2026-02-05T13:40:02.457067"
}