# Local secrets (optional). Copy to .env and fill in values.
# .env is in .gitignore — never commit it or store secrets in GitHub.
#
# PyPI / Twine (required for scripts/upload_to_pypi.ps1 -Repository pypi)
# Get token: https://pypi.org/manage/account/token/
# Add exactly one of these lines with your token (no spaces around =):
TWINE_PASSWORD=
# PYPI_API_TOKEN=
# PYPI_TOKEN=
