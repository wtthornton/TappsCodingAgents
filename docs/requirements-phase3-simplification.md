{
  "description": "Implement Phase 3 Simplification: 1) Simplify State Management - single progress file instead of multiple files (progress, results, workflow state), unified result format, reduce state synchronization issues, 2) Temp Directory Option - use temp directories for non-git use cases instead of worktrees, worktrees only when git operations needed, reduce git dependency, 3) Reduce Monitoring Scripts - consolidate multiple monitoring scripts (monitor_background_agents.py, monitor_status.py, check_background_agents.py) into single unified script, remove redundant scripts",
  "instruction": {
    "agent_name": "analyst",
    "command": "analyze-requirements",
    "prompt": "Analyze the following requirement description and extract detailed requirements.\n\nDescription:\nImplement Phase 3 Simplification: 1) Simplify State Management - single progress file instead of multiple files (progress, results, workflow state), unified result format, reduce state synchronization issues, 2) Temp Directory Option - use temp directories for non-git use cases instead of worktrees, worktrees only when git operations needed, reduce git dependency, 3) Reduce Monitoring Scripts - consolidate multiple monitoring scripts (monitor_background_agents.py, monitor_status.py, check_background_agents.py) into single unified script, remove redundant scripts\n\n\n\nPlease provide:\n1. Functional Requirements (what the system should do)\n2. Non-Functional Requirements (performance, security, scalability, etc.)\n3. Technical Constraints\n4. Assumptions\n5. Open Questions\n\nFormat as structured JSON with sections.",
    "parameters": {
      "description": "Implement Phase 3 Simplification: 1) Simplify State Management - single progress file instead of multiple files (progress, results, workflow state), unified result format, reduce state synchronization issues, 2) Temp Directory Option - use temp directories for non-git use cases instead of worktrees, worktrees only when git operations needed, reduce git dependency, 3) Reduce Monitoring Scripts - consolidate multiple monitoring scripts (monitor_background_agents.py, monitor_status.py, check_background_agents.py) into single unified script, remove redundant scripts"
    }
  },
  "skill_command": "@analyst analyze-requirements --description \"Implement Phase 3 Simplification: 1) Simplify State Management - single progress file instead of multiple files (progress, results, workflow state), unified result format, reduce state synchronization issues, 2) Temp Directory Option - use temp directories for non-git use cases instead of worktrees, worktrees only when git operations needed, reduce git dependency, 3) Reduce Monitoring Scripts - consolidate multiple monitoring scripts (monitor_background_agents.py, monitor_status.py, check_background_agents.py) into single unified script, remove redundant scripts\""
}