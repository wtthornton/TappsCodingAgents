# Step 5 — Implementation (execute the recommendations)

## Files to edit

- `docs/CURSOR_RULES_SETUP.md`
  - Add: “Rule layering & scoping (recommended pattern)”
  - Include: example `.mdc` frontmatter with `globs` and guidance on `alwaysApply`

- `docs/CUSTOM_SKILLS_GUIDE.md`
  - Add: “Skill design checklist (reliability)”
  - Add: “Recommended skill bundle layout”
  - Add: “Progressive disclosure guidance”

