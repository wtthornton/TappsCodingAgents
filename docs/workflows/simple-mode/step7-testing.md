# Step 7 — Testing (docs-focused)

## Validation steps

- Ensure the new sections render cleanly in Markdown (headings, lists, code fences).
- Ensure new snippets match actual repo structure and don’t introduce contradictory guidance.
- Spot check that the updated docs don’t conflict with:
  - `docs/CURSOR_RULES_SETUP.md`
  - `docs/SIMPLE_MODE_GUIDE.md`
  - `.cursor/rules/*.mdc`

